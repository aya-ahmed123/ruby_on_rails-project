<%= div_for(comment) do  %>
    <p>
            <strong>
          Posted <%= time_ago_in_words(comment.created_at) %>
            </strong><br>
            <%= h(comment.comment_content) %>
    </p>
<% end %>